import { test, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import Page from './page'

test('Car Photography page renders correctly', () => {
  render(<Page />)
  // Add specific tests based on your Car Photography page content
}) 