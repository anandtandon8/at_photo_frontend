import { test, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import Page from './page'

test('Portrait Photography page renders correctly', () => {
  render(<Page />)
  // Add specific tests based on your Portrait Photography page content
}) 