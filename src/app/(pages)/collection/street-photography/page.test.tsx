import { test, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import Page from './page'

test('Street Photography page renders correctly', () => {
  render(<Page />)
  // Add specific tests based on your Street Photography page content
}) 