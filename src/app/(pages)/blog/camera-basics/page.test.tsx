import { test, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import Page from './page'

test('Camera Basics page renders correctly', () => {
  render(<Page />)
  // Add specific tests based on your Camera Basics page content
}) 