import { test, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import Page from './page'

test('Rules of Composition page renders correctly', () => {
  render(<Page />)
  // Add specific tests based on your Rules of Composition page content
}) 