import { test, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import Page from './page'

test('How to Start page renders correctly', () => {
  render(<Page />)
  // Add specific tests based on your How to Start page content
}) 